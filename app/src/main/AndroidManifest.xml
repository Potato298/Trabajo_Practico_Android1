<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itemscsgo.juan.itemscsgo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WeaponCases" />
        <activity android:name=".WeaponShow" />
        <activity android:name=".Collections" />
        <activity android:name=".Weapons" />
        <activity android:name=".WeaponCaseResults" />
        <activity
            android:name=".weapon_collection_results"
            android:label="@string/title_activity_weapon_collection_results"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".weapons_collections_results" />
        <activity android:name=".Weapon_Guide"></activity>
    </application>

</manifest>